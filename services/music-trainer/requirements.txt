# ACE-Step v1 training deps (from ace-step/ACE-Step requirements.txt).
# torch triple pinned to cog v0.9.8's compatibility matrix row for CUDA 12.1
# (torch 2.3.0 / vision 0.18.0 / audio 2.3.0) — we MUST use cog 0.9.8 because
# it's the version Replicate's own CI action pins, and the only line that
# accepts the API token for the r8.im registry push (newer cogs demand a
# separate CLI auth token). numpy<2: torch 2.3-era wheels crash on numpy 2.x.
# gradio kept so `import acestep` never trips on a UI import.
datasets==3.4.1
# 0.33.1 exactly (run d4jw2m5w… failed): newer diffusers imports
# torch.nn.attention.flex_attention — a torch 2.5 API; this image is the
# cog-0.9.8 matrix ceiling torch 2.3. ACE-Step v1 needs >=0.33.0; 0.33.1 is it.
diffusers==0.33.1
librosa==0.11.0
loguru==0.7.3
matplotlib==3.10.1
numpy<2
# 0.2.2 not 0.3.0: py3langid 0.3.0 requires numpy>=2, which clashes with the
# torch-2.3-era numpy<2 guard. Same classify() API; it's only lyric lang-ID.
py3langid==0.2.2
pypinyin==0.53.0
pytorch_lightning==2.5.1
soundfile==0.13.1
torch==2.3.0
torchvision==0.18.0
torchaudio==2.3.0
tqdm
transformers==4.50.0
hangul-romanize==0.1.0
num2words==0.5.14
# PYDANTIC-1 WORLD (live failure 7bjtwdrh…): cog 0.9.8's runtime is pydantic v1;
# spacy 3.8's thinc imports pydantic-2-only API (GetCoreSchemaHandler) and the
# trainer died at import. spacy 3.7.x + thinc 8.2.x run on pydantic 1. gradio
# dropped for the same reason (v4+ demands pydantic 2; nothing headless uses it).
pydantic>=1.10,<2
spacy==3.7.4
accelerate==1.6.0
cutlet
fugashi[unidic-lite]
click
peft==0.15.2
tensorboard
tensorboardX
